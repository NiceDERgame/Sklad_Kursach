<Page x:Class="Sklad_Kursach.Pages.Inventory_Page"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Склад" Loaded="Page_Loaded">

    <Grid Background="{StaticResource BrushBackground}">
        <Grid.Resources>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#78909C"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Height" Value="45"/>
                <Setter Property="Padding" Value="10,0"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="BorderBrush" Value="#EEEEEE"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
            </Style>

            <Style TargetType="DataGridRow">
                <Setter Property="Height" Value="40"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#F5F7FA"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="#E3F2FD"/>
                        <Setter Property="Foreground" Value="{StaticResource BrushPrimaryDark}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="DataGridCell">
                <Setter Property="Padding" Value="10,0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridCell">
                            <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ShelfLifeTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#D32F2F"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="40,40,40,20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="⬅" Width="50" Height="50" Margin="0,0,20,0" Style="{StaticResource BtnStyle}" Click="GoBack_Click" FontSize="24" Padding="0,0,0,4"/>
                <TextBlock Text="📦" FontSize="32" Margin="0,0,15,0" VerticalAlignment="Center"/>
                <StackPanel>
                    <TextBlock Text="ИНВЕНТАРИЗАЦИЯ" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushPrimaryDark}"/>
                    <TextBlock Text="Управление товарами на складе" FontSize="14" Foreground="{StaticResource BrushTextSecondary}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel x:Name="AdminControlPanel" Orientation="Horizontal" Margin="70,20,0,0" Visibility="Collapsed">
                <Button Content="✎ Редактировать" Style="{StaticResource BtnStyle}" Background="#FFA000" Width="160" Height="40" Margin="0,0,15,0" Click="EditProduct_Click"/>
                <Button Content="🗑 Удалить партию" Style="{StaticResource BtnStyle}" Background="#D32F2F" Width="160" Height="40" Click="DeleteProduct_Click"/>
            </StackPanel>
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <StackPanel Margin="40,0,40,40">

                <Border Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,25" BorderBrush="#EEEEEE" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="20" Opacity="0.08" ShadowDepth="4" Color="Black"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Background="#E8F5E9" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                <TextBlock Text="🟢 Новый товар" FontSize="14" FontWeight="Bold" Foreground="#2E7D32"/>
                            </Border>
                            <TextBlock Grid.Column="1" Text="Нажмите на товар, чтобы переместить его на полку" 
                                       VerticalAlignment="Center" Margin="15,0,0,0" Foreground="#90A4AE" FontStyle="Italic"/>
                        </Grid>

                        <DataGrid Grid.Row="1" x:Name="NewItemsGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                                  SelectionMode="Single" SelectionChanged="NewItemsGrid_SelectionChanged"
                                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F5F5F5" VerticalGridLinesBrush="Transparent"
                                  Background="Transparent" BorderThickness="0" HeadersVisibility="Column"
                                  AlternatingRowBackground="#FAFAFA">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Название товара" Binding="{Binding ProductName}" Width="2*"/>
                                <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="150"/>
                                <DataGridTextColumn Header="Кол-во" Binding="{Binding TotalCount}" Width="100"/>
                                <DataGridTextColumn Header="Остаток срока" Binding="{Binding DaysLeft}" Width="150" ElementStyle="{StaticResource ShelfLifeTextStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                            <Button x:Name="InfoBtn" Content="ℹ Инфо" Background="White" Foreground="#546E7A" BorderBrush="#CFD8DC" BorderThickness="1" 
                                    Width="100" Height="40" Margin="0,0,10,0" Click="InfoBtn_Click"/>
                            <Button x:Name="SortBtn" Content="📍 Разместить" Width="140" Height="40" Click="SortBtn_Click" IsEnabled="False"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,20" BorderBrush="#EEEEEE" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="20" Opacity="0.08" ShadowDepth="4" Color="Black"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Margin="0,0,0,15">
                            <Border Background="#FFF8E1" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left">
                                <TextBlock Text="🟡 Товар на складе" FontSize="14" FontWeight="Bold" Foreground="#F57F17"/>
                            </Border>
                        </Grid>

                        <DataGrid Grid.Row="1" x:Name="InStockGrid" AutoGenerateColumns="False" IsReadOnly="True"
                                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F5F5F5" VerticalGridLinesBrush="Transparent"
                                  Background="Transparent" BorderThickness="0" HeadersVisibility="Column"
                                  AlternatingRowBackground="#FAFAFA">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Название товара" Binding="{Binding ProductName}" Width="2*"/>
                                <DataGridTextColumn Header="Ячейка" Binding="{Binding StorageCell}" Width="100" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Кол-во" Binding="{Binding TotalCount}" Width="100"/>
                                <DataGridTextColumn Header="Остаток срока" Binding="{Binding DaysLeft}" Width="150"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Grid x:Name="EditOverlay" Grid.RowSpan="2" Visibility="Collapsed">
            <Border Background="Black" Opacity="0.5" MouseDown="CancelEdit_Click"/>

            <Border Background="White" Width="400" VerticalAlignment="Center" HorizontalAlignment="Center" CornerRadius="12" Padding="30">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" Opacity="0.3"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Редактирование партии" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" Foreground="{StaticResource BrushPrimary}"/>

                    <TextBlock Text="Название товара (в каталоге)" Foreground="#78909C" Margin="0,0,0,5"/>
                    <TextBox x:Name="EditNameTb" Style="{StaticResource InputStyle}" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Количество (шт)" Foreground="#78909C" Margin="0,0,0,5"/>
                            <TextBox x:Name="EditCountTb" Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Срок хранения (часов)" Foreground="#78909C" Margin="0,0,0,5"/>
                            <TextBox x:Name="EditLifeTb" Style="{StaticResource InputStyle}"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
                        <Button Content="Отмена" Background="Transparent" Foreground="Gray" Click="CancelEdit_Click" Margin="0,0,10,0"/>
                        <Button Content="Сохранить" Style="{StaticResource BtnStyle}" Click="SaveEdit_Click" Width="120"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

    </Grid>
</Page>