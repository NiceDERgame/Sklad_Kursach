<Page x:Class="Sklad_Kursach.Pages.Logs_Page"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Логи" d:DesignHeight="700" d:DesignWidth="1100"
      Loaded="Page_Loaded">

    <Grid Background="{StaticResource BrushBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="40,40,40,20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="⬅" ToolTip="Вернуться в меню" Width="50" Height="50" Margin="0,0,20,0" 
                        Style="{StaticResource BtnStyle}" Click="GoBack_Click" FontSize="24" Padding="0,0,0,4"/>

                <TextBlock Text="📜" FontSize="32" Margin="0,0,15,0" VerticalAlignment="Center"/>
                <StackPanel>
                    <TextBlock Text="ЖУРНАЛ ДЕЙСТВИЙ" FontSize="28" FontWeight="Bold" Foreground="{StaticResource BrushPrimaryDark}"/>
                    <TextBlock Text="История операций сотрудников" FontSize="14" Foreground="#78909C"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Margin="40,0,40,40">
            <DataGrid x:Name="LogsGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      Background="Transparent" BorderThickness="0"
                      RowHeight="40" ColumnHeaderHeight="45">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Время" Binding="{Binding ActionTime, StringFormat=dd.MM.yyyy HH:mm}" Width="160"/>
                    <DataGridTextColumn Header="Сотрудник" Binding="{Binding EmployeeFIO}" Width="200"/>
                    <DataGridTextColumn Header="Действие" Binding="{Binding ActionType}" Width="150" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Товар" Binding="{Binding ProductName}" Width="*"/>
                    <DataGridTextColumn Header="Ячейка" Binding="{Binding StorageCell}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>